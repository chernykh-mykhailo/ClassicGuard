version: '3.8'

services:
  classicguard:
    build: .
    container_name: classicguard-bot
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
